---
# Inventaire exemple — Staging Element Web
# Adapter l'IP à votre infrastructure.

all:
  children:
    element_web:
      hosts:
        vm-element-staging:
          ansible_host: 192.168.1.80
          ansible_user: devops
